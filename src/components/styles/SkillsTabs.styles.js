import { Tab, styled } from "@mui/material";

export const TabStyled = styled(Tab)`
        color: white;
        text-transform: none;
        font-family: Lato;
        font-size: 2rem;
        &.Mui-selected {
         color: #1bbafd;
        }
    `
